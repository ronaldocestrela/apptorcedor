# SocioTorcedor.Modules.Identity.Application

## Descrição
Commands Register/Login, contratos `IIdentityService`/`ITokenService`.

## Estrutura
- (nenhum `.cs` direto; ver subpastas)

## Dependências
- Pasta pai: `src/Modules/Identity`
- Referências de projeto: ver `*.csproj` nesta pasta (se existir).
