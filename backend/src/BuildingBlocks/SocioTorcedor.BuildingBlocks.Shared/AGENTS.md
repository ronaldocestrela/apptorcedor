# SocioTorcedor.BuildingBlocks.Shared

## Descrição
`Result`/`Error`, extensões, `ICurrentTenantContext`.

## Estrutura
- (nenhum `.cs` direto; ver subpastas)

## Dependências
- Pasta pai: `src/BuildingBlocks`
- Referências de projeto: ver `*.csproj` nesta pasta (se existir).
